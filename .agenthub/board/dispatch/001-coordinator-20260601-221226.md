---
author: coordinator
timestamp: 2026-06-01T22:12:26+08:00
channel: dispatch
parent: null
---

## Session 20260601-221226

Task: refactor `shuqian-nav` according to `docs/refactor-roadmap.md` phase 1.

The coordinator will keep this session focused on small, verifiable changes. The current first target is not a full redesign; it is the architecture baseline that lets the redesign happen safely.

## Shared Rules

- Preserve the no-build Cloudflare Pages deployment.
- Do not split the Pages project.
- Keep homepage public and admin pages protected.
- Do not duplicate `components/header.html` into pages.
- Prefer small commits that keep `npm test` green.
- Do not include unrelated dirty worktree changes.

## Agent Assignments

### Agent A: Architecture

Build and maintain a loading matrix. Verify that homepage-only scripts live under `public/js/home/` and admin pages do not load them.

### Agent B: Homepage

Keep `/` usable without login. Verify search, category rail, sorting, cards, favicon clarity, and admin action gating.

### Agent C: Admin Menu

Audit `site-menu.js` and `components/header.html`. Produce a menu structure that avoids duplicate actions and gives correct active state.

### Agent D: Management Pages

Audit `bookmarks-manage.html`, `categories.html`, `deleted-bookmarks.html`, and `import.html`. Identify duplicated controls and missing unified states.

### Agent E: API/Security

Verify public GET endpoints and protected write/system endpoints. Add or maintain `tests/api-core.test.js` coverage.

### Agent F: QA

Run browser checks for public homepage and protected admin routes. Capture failures with reproducible steps.

## First Coordinator Deliverable

- Add `docs/refactor-loading-matrix.md`.
- Wire homepage script tags to the staged `public/js/home/` files.
- Run syntax checks for moved scripts.
- Run the narrow browser/API checks needed to prove the baseline.
